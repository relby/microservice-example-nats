// Subjects
export const StorageMethods = {
  Test: {
    find: 'storage.test.find',
  },
  Product: {
    find: 'storage.product.find',
    findOneById: 'storage.product.findOneById',
    createOne: 'storage.product.createOne',
    updateOne: 'storage.product.updateOne',
    deleteOne: 'storage.product.deleteOne'
  }
}
